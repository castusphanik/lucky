import { Typography } from "@mui/material";

const Landing = () => {
  return (
    <main style={{ margin: 40 }}>
      <Typography>Tenleasing</Typography>
    </main>
  );
};

export default Landing;
